# Backlink Hunter — core runtime dependencies (pinned/bounded, Windows Py3.11+)

streamlit>=1.31,<2.0

requests>=2.31,<3.0

tldextract>=5.1,<6.0

# --- Optional extras (install as needed) ---

# Parquet dataset import:

#   pip install "pyarrow>=15.0,<18.0"

# PostgreSQL backend (future/optional):

#   pip install "psycopg[binary]>=3.1,<4.0"

# Development / testing:

#   pip install "pytest>=8.0,<9.0" "pytest-mock>=3.12,<4.0"
