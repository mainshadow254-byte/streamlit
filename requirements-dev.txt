-r requirements.txt

pytest>=8.0,<9.0

pytest-mock>=3.12,<4.0

pyarrow>=15.0,<18.0PART: pyproject.tomlTomlCopied!Copy[build-system]  

requires = ["setuptools>=68", "wheel"]  

build-backend = "setuptools.build_meta"  

  

[project]  

name = "backlink-hunter"  

version = "2.0.0"  

description = "Local, truthful, domain-only reverse-backlink discovery and verification."  

readme = "README.md"  

requires-python = ">=3.11"  

license = { text = "MIT" }  

authors = [{ name = "mainshadow254-byte" }]  

keywords = ["backlinks", "seo", "common-crawl", "warc", "wat", "reverse-index"]  

  

dependencies = [  

    "streamlit>=1.31,<2.0",  

    "requests>=2.31,<3.0",  

    "tldextract>=5.1,<6.0",  

]  

  

[project.optional-dependencies]  

parquet = ["pyarrow>=15.0,<18.0"]  

postgres = ["psycopg[binary]>=3.1,<4.0"]  

dev = [  

    "pytest>=8.0,<9.0",  

    "pytest-mock>=3.12,<4.0",  

    "pyarrow>=15.0,<18.0",  

]  

  

[project.scripts]  

backlink-hunter = "backlink_hunter:main"  

  

[tool.pytest.ini_options]  

testpaths = ["tests"]  

markers = [  

    "integration: tests that require live network access to Common Crawl",  

]  

  

[tool.setuptools]  

py-modules = ["backlink_hunter", "streamlit_app"]  

  

[tool.setuptools.packages.find]  

include = ["backlink_hunter_core*"]  PART: .gitignoreTextCopied!Copy# Databases / generated indexes — never commit real indexes

*.db

*.db-wal

*.db-shm

backlinks.db

config.json

# Logs

logs/

*.log

# Test fixtures are generated locally

tests/fixtures/

# Temp/export artifacts

blh_*.tmp

*.tmp

# Python

__pycache__/

*.py[cod]

*.egg-info/

.eggs/

build/

dist/

.venv/

venv/

env/

.pytest_cache/

.mypy_cache/

.coverage

htmlcov/

# OS / editor

.DS_Store

Thumbs.db

.idea/

.vscode/✅ Dependencies + packaging + gitignore complete.Now the final piece — the README.
